name: My First Workflow

on: 
  push:
    branches:
      - master

jobs:
  say-hello:
    runs-on: ubuntu-latest
    
    steps:
      - name: Say hello
        run: echo "Hello! My workflow is running!"
      
      - name: Show the date
        run: date
      
      - name: List files
        run: ls -la